PROMPT 7C-P2 — Dashboard Parte 2 (completar dashboard)

[FASE 7C PARTE 2 — DASHBOARD: SECCIÓN INFERIOR CON DATOS REALES]
Completa el dashboard agregando la sección inferior. Ya existe la Parte 1 con hero, KPIs y agenda lateral. Ahora agrega debajo de los KPIs, dentro de .dashboard-right, las siguientes secciones. Conecta todo al backend real, sin mocks.
Importa en el componente: HttpClient via inject, usa environment.apiUrl. Llama a los endpoints al inicializar con ngOnInit.
Datos a cargar desde la API:

GET /api/v1/citas/?estado=confirmada y ?estado=pendiente → para próximas citas
GET /api/v1/terapeutas/?activo=true → para terapeutas activos
GET /api/v1/citas/reportes/servicios-populares → para servicios más reservados
GET /api/v1/productos/?stock_bajo=true → para inventario bajo

Reemplaza también los KPIs hardcodeados de la Parte 1 con datos reales:

Ingresos del día: suma los total de citas con fecha = hoy y estado = completada
Ocupación: cabinas con citas activas hoy vs total cabinas (GET /api/v1/cabinas/)
Citas de hoy: filtra citas por fecha = hoy
Nuevos clientes: GET /api/v1/usuarios/?rol=cliente cuenta los de created_at de hoy

Sub-sección .proximas-citas:

Título "Próximas citas" con link "Ver todas →" alineado a la derecha, color: var(--color-primary); font-size: 13px
Tabla sin bordes pesados: width: 100%; border-collapse: collapse
Encabezados: HORA / CLIENTE / SERVICIO / TERAPEUTA / SALA / ESTADO en font-size: 11px; font-weight: 600; color: var(--color-text-aux); text-transform: uppercase; padding: 0 12px 12px
Cada fila: border-bottom: 1px solid var(--color-border); padding: 14px 12px

Avatar circular del cliente: iniciales, 32px, background: var(--color-bg-secondary)
Nombre del cliente en font-size: 14px; font-weight: 500
Badge de estado con colores de styles.scss: border-radius: var(--radius-pill); padding: 4px 10px; font-size: 12px; font-weight: 500

Muestra máximo 5 citas ordenadas por hora.

Sub-sección .terapeutas-activos:

display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px
Cada terapeuta: card con avatar circular 56px (iniciales del nombre), nombre en font-size: 13px; font-weight: 500; text-align: center; margin-top: 8px, especialidad en font-size: 12px; color: var(--color-text-secondary), punto verde 8px en esquina inferior derecha del avatar indicando activo.

Sub-sección .bottom-grid:
scssdisplay: grid;
grid-template-columns: 1fr 1fr 1fr;
gap: 20px;
margin-top: 24px;
Card "Servicios más reservados":

Lista de hasta 4 servicios del endpoint de reportes
Cada ítem: ícono genérico de spa 32px (SVG lotus o similar) + nombre del servicio + "X reservas" + mini sparkline (línea SVG simple ascendente decorativa, no datos reales, solo visual)

Card "Inventario bajo":

Lista de productos con stock <= stock_minimo del endpoint
Cada ítem: nombre + "Stock: X unidades" en color: var(--color-warning) si está bajo, color: var(--color-error) si es 0
Link "Ver inventario →" al fondo

Card "Acciones rápidas":

Grid 2x3 de botones de acción
Cada botón: ícono SVG 20px + texto, background: var(--color-bg); border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: 16px; cursor: pointer; transition: all 0.2s; font-size: 13px; font-weight: 500
Hover: background: var(--color-primary-light); border-color: var(--color-primary)
6 acciones con routerLink o (click):

"Nueva cita" → navega a /agenda
"Nuevo cliente" → navega a /clientes
"Cobrar servicio" → navega a /agenda
"Venta de producto" → navega a /inventario
"Uso de inventario" → navega a /inventario
"Generar reporte" → navega a /reportes

Reglas críticas de estructura de archivos:

Todo el HTML va en dashboard.component.html, nada inline en el .ts
Todo el CSS va en dashboard.component.scss, sin styles inline en el .ts
El .ts solo tiene lógica: signals, inject, ngOnInit con llamadas HTTP
Usa signal<T[]>([]) para cada lista de datos, actualiza con .set() en el subscribe
Maneja estado de carga con cargando = signal(true) y muestra skeleton o spinner mientras cargan los datos

PROMPT 7D — Adaptar vistas de Stitch a Angular

[ADAPTACIÓN DE VISTAS STITCH → ANGULAR]
Tengo las siguientes vistas diseñadas en Google Stitch (que exporta HTML/CSS con Tailwind). Necesito que las adaptes al stack del proyecto: Angular 17+ standalone, SCSS con las variables CSS de styles.scss (sin Tailwind), conectadas al backend FastAPI real.
Regla de oro para cada vista:

Elimina todas las clases de Tailwind (flex, p-4, text-sm, etc.) y reemplázalas por SCSS usando las variables --color-_, --font-_, --radius-_, --shadow-_ definidas en styles.scss
Separa SIEMPRE en tres archivos: .ts (lógica), .html (template), .scss (estilos). Nunca mezcles template o estilos dentro del .ts
Cada componente debe ser standalone: true con sus imports explícitos
Conecta a la API real usando los servicios de core/services/. Sin mocks, sin datos hardcodeados
Los formularios usan ReactiveFormsModule con FormBuilder
Las listas usan @for ... track item.id
Los condicionales usan @if / @else

Paleta de colores que reemplaza a Tailwind (ya definida en styles.scss):
Fondo: var(--color-bg) = #F6F1EA
Superficies: var(--color-surface) = #FFFFFF
Primario: var(--color-primary) = #C96A2B
Primario hover: var(--color-primary-hover) = #B85D24
Texto: var(--color-text) = #2B2B2B
Texto secundario: var(--color-text-secondary) = #6B625C
Bordes: var(--color-border) = #E7DDD3
Éxito: var(--color-success) = #6F9F78
Error: var(--color-error) = #C96A6A
Vistas a adaptar (pégalas una por una después de este prompt):

Dashboard (/dashboard) — ya hecho en 7C, referencia de estilo
Login (/login) — ya hecho en 7B, referencia de estilo
Landing Page (/) — ruta pública, sin auth guard. Componente LandingComponent. No conecta al backend. Solo visual con routerLink al login.
Registro de usuario (/registro) — ver sección específica abajo
Agenda (/agenda) — ver sección específica abajo

Para cada vista que pegues, genera los tres archivos completos y lista los endpoints que consume.
